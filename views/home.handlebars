<script src="http://localhost:4000/socket.io/socket.io.js"></script>
<script>
    // Create SocketIO instance, connect

    var socket = new io.Socket();

    socket.connect('http://localhost:8080');

    // Add a connect listener
    socket.on('connect',function() {
      console.log('Client has connected to the server!');
    });
    // Add a connect listener
    socket.on('message',function(data) {
      console.log('Received a message from the server!',data);
    });
    // Add a disconnect listener
    socket.on('disconnect',function() {
      console.log('The client has disconnected!');
    });

    // Sends a message to the server via sockets
    function sendMessageToServer(message) {
      socket.send(message);
    };
</script>




<title>SoundPlow</title>

<p style = "font-size:50px">SoundPlow</p>

<span><a href="/allUsers">Users</a></span>
<br>
<span><a href="/artist">Artists</a></span>
<br>
<span><a href="/concerts">Concerts</a></span>
<br>
<span><a href="/location">Locations</a></span>
<br>
<span><a href="/descriptions">Description Page</a></span>
<br>
<!-- <span><a href="/chat">Live Chat</a></span> -->